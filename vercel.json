{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "app.py"
        }
    ],
    "installCommand": "pip install -r requirements.txt && pip uninstall -y asyncio || true"
}